/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "gxohpvyv9zbtt65m.public.blob.vercel-storage.com",
        },
      ],
    },
  };

export default nextConfig;
